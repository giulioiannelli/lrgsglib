# IsingModel C++ Library

`IsingModel.cpp` implements the core Ising model and exposes a Python
module via Boost.Python. Building via `make c-make` places the compiled
extension under `src/lrgsglib/Ccore/bin`.
